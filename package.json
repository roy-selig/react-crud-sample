{
  "name": "untitled",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^3.6.1",
    "json-server": "^0.14.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "concurrently \"json-server --watch data/db.json --port 3004\" \"react-scripts start\"",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
